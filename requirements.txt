# BK_Estudo_Proteção v2 — Dependências
# Instalar com: pip install -r requirements.txt

# ─── Streamlit ────────────────────────────────────────────────────────────────
streamlit>=1.31.0

# ─── Framework Web (FastAPI — mantido para compatibilidade com módulos app/) ──
fastapi==0.115.5
uvicorn==0.32.1          # sem [standard] — evita uvloop/httptools (C extensions)
python-multipart==0.0.12
jinja2==3.1.4

# ─── Banco de dados ───────────────────────────────────────────────────────────
sqlalchemy==2.0.36       # sem [asyncio] — Streamlit usa apenas sync
alembic==1.14.0
psycopg2-binary==2.9.10  # PostgreSQL sync (Neon)
aiosqlite==0.20.0        # SQLite async (dev local)
# asyncpg removido — requer compilação C; Streamlit usa psycopg2

# ─── Autenticação ─────────────────────────────────────────────────────────────
pyjwt==2.9.0
passlib[bcrypt]==1.7.4
bcrypt==4.2.0

# ─── Configuração ─────────────────────────────────────────────────────────────
pydantic==2.9.2
pydantic-settings==2.6.1
python-dotenv==1.0.1

# ─── Cálculos e gráficos ──────────────────────────────────────────────────────
numpy==2.1.3
matplotlib==3.9.2
scipy==1.14.1

# ─── Relatórios ───────────────────────────────────────────────────────────────
openpyxl==3.1.5
Pillow==11.0.0
# weasyprint — requer libcairo/libpango; não suportado no Streamlit Cloud

# ─── Utilitários ──────────────────────────────────────────────────────────────
email-validator==2.2.0
